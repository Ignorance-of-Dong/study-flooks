import React, {useState, useContext} from 'react'
import Ptemp from '../Ptemp/index'

function Buttons(props) {
  // const val = useContext(Context)
  return (
    <>
      <p>Msg</p>
      <Ptemp></Ptemp>
    </>
  )
}

export default Buttons